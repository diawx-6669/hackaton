# Бэкенд CampusLens AI. Собирается и запускается одинаково локально и на Render/Railway.
FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app ./app

# Кеш пишется на диск: каталог должен существовать и быть доступен на запись.
RUN mkdir -p /app/data/cache
ENV CAMPUSLENS_CACHE_DIR=/app/data/cache

EXPOSE 8000

# $PORT задаёт хостинг (Render/Railway), локально по умолчанию 8000.
CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
