# CampusLens AI

**Хакатон LOCUS 2026, кейс 1.** Веб-сервис: вводишь название университета — за считанные
секунды получаешь **проверенный визуальный профиль кампуса**: фотографии из открытых
источников с автором, лицензией, кликабельной ссылкой на источник и разбором улик,
по которым выставлен Confidence Score.

Ключевой принцип: **ничего не захардкожено**. В репозитории нет ни одной картинки-заглушки
и ни одного заранее заготовленного результата — всё ищется на лету в Wikidata и Wikimedia
Commons. Если данных нет, сервис так и пишет, а не придумывает.

---

## Статус

| Шаг | Что | Статус |
|-----|-----|--------|
| 1 | `/api/resolve` — поиск вуза через Wikidata (wbsearchentities + SPARQL), нечёткий поиск и синонимы | готово |
| 2 | Сборщик Wikimedia Commons: категория вуза (P373) + geosearch в радиусе 1 км | готово |
| 3 | `/api/profile` — SSE-воронка этапов с таймером, параллельные сборщики, общий таймаут 25 с | готово |
| 4 | Дедупликация pHash (imagehash) + CLIP-эмбеддинги | готово, pHash по миниатюрам работает; сравнение CLIP-эмбеддингов — нет |
| 5 | Классификация по категориям + класс «мусор» | частично: работает классификатор **по метаданным** Commons; CLIP zero-shot ещё не подключён |
| 6 | Confidence Score с разбором улик | готово, шесть улик: геотег, домен, привязка к вузу, название в метаданных, свежесть, повтор в источниках |
| 7 | Фронтенд: выбор при неоднозначности, воронка, галерея, фильтры, карточка с уликами, мобилка | готово |
| 8 | Карта OSM, сравнение вузов, кеш | готово |
| 8 | Фото и текст с сайта вуза | готово, с соблюдением robots.txt |
| 6 | LLM-описание кампуса со ссылками | готово, включается ключом `ANTHROPIC_API_KEY` |
| 8 | «Жизнь в радиусе 15 минут» по OSM | готово, Overpass: транспорт, еда, продукты, аптеки, спорт, парки, общежития |
| 8 | Инфраструктура района | готово, освещённые улицы, фонари, переходы, ближайшая полиция — счётчики, без сводной оценки |
| 8 | Время в пути | готово, OSRM (автомобиль) + честная оценка по прямой для пешего участка |
| — | События вуза | готово, группировка найденных фото по признакам события и годам |
| — | Видео с Commons | готово, только свободные файлы Wikimedia; сторонние площадки не трогаем |
| — | Стоимость | готово, дословные цитаты с сайта вуза со ссылкой; своих цифр не придумываем |
| — | Категория «Питание и быт» | готово, столовые, буфеты, кухни, прачечные |
| — | Браузерные тесты (Playwright) | готово, 40 проверок в двух профилях: десктоп и телефон |
| — | Логотип и интро | готово, логотип в шапке, вкладке, iOS-иконке и превью для соцсетей; ролик на первом заходе |
| — | Панель разделов профиля | готово, липнет под шапкой, ведёт к блокам, показывает только собранные |
| — | Блок «О вузе» | готово, факты из Wikidata (основан, студенты, сотрудники) — работает и без LLM |
| — | Аудиообзор | готово, синтез речи браузера читает уже собранное описание |
| 8 | Поисковый API картинок (Brave/SerpAPI) | впереди |
| 10 | Скрипт оценки качества: precision/recall | готово, `python -m eval.run_eval` |
| — | Вход в аккаунт, сайт закрыт для гостей | готово, `/login`, демо-аккаунт для жюри |
| — | Заявка «не нашли свой вуз» | готово, `POST /api/subscribe` |
| — | Фото от студентов и бонусы | готово, `/my`, отдельно от проверенной галереи |

---

## Стек

- **Frontend** — Next.js 16 (App Router, React 19) + TailwindCSS 4, TypeScript. Папка `web/`.
- **Backend** — Python 3.11, FastAPI + uvicorn, httpx (async), sse-starlette, rapidfuzz. Папка `api/`.
- **Тесты** — pytest + respx: все внешние API замоканы, тесты не ходят в сеть.

---

## Архитектура

```
Браузер (web/)
   │  GET /api/resolve?q=КБТУ                → список кандидатов, ambiguous?
   │  GET /api/profile?id=Q1798175  (SSE)    → поток этапов + финальный профиль
   ▼
FastAPI (api/)
   │
   ├─ services/textmatch.py   нормализация, транслитерация, синонимы, нечёткое сравнение
   ├─ services/wikidata.py    wbsearchentities (ru/en/kk) → SPARQL → University
   ├─ services/commons.py     категория P373 (обход подкатегорий) ┐ параллельно
   │                          geosearch в радиусе от координат    ┘ (asyncio)
   ├─ services/dedup.py       склейка одинаковых файлов, слияние источников
   ├─ services/classify.py    категория фото по метаданным + отсев мусора
   ├─ services/evidence.py    улики + Confidence Score + пороги
   ├─ services/cache.py       TTL-кеш: память + файл, переживает рестарт
   └─ pipeline.py             конвейер и генерация SSE-этапов с таймером
```

**Конвейер (`pipeline.py`)**

```
collecting → resolved → found → deduped → classified → rejected → verified → done
```

Каждый этап уходит клиенту отдельным SSE-событием с полем `elapsed_ms` — отсюда живая
воронка и таймер на фронте. Сборщики стартуют одновременно (`asyncio.create_task` +
`asyncio.wait`), общий дедлайн — `CAMPUSLENS_TOTAL_TIMEOUT` (25 с при требовании «до 30»).
Что не успело — отменяется, ответ не блокируется, а причина попадает в `warnings`.

**Первые фото — за ~3 секунды.** Конвейер отдаёт результат трижды, а не один раз:

1. `CAMPUSLENS_FIRST_PAINT_BUDGET` (по умолчанию 3 с) — событие `verified` с тем,
   что успели собрать быстрые источники. Перцептивная дедупликация сюда не входит:
   она качает миниатюры, то есть это единственная часть анализа, которая ходит в сеть.
2. Второе событие `verified` — когда доехали остальные источники и отработал pHash.
3. `done` — то же самое плюс описание от LLM, `partial: false`.

Фронтенд перерисовывает профиль на каждом событии с `payload`, поэтому галерея
появляется почти сразу и дополняется на глазах. Медленный источник больше не
задерживает показ — он просто добавляет фото позже. Это проверяется тестом
`test_first_paint_does_not_wait_for_slow_source`.

**Вузы всего мира.** Списка вузов в проекте нет и не было — ищем в Wikidata, где
их десятки тысяч. Язык поиска подбирается по письменности запроса: кириллица →
ru/kk/uk/sr/bg, латиница → en/de/fr/es/pt/it и далее, есть наборы для арабского,
китайского, японского, корейского, деванагари, греческого, иврита, тайского,
грузинского и армянского. Английский входит в каждый набор. Число обращений
ограничено (`MAX_SEARCH_REQUESTS`), иначе широкий набор языков съел бы весь бюджет.

**Если SPARQL недоступен.** `query.wikidata.org` регулярно отвечает 403 под нагрузкой.
При ошибке `fetch_details()` переключается на Action API (`wbgetentities`) и достаёт
оттуда P625/P856/P373/P17/P131/P154/P571, а названия города и страны — вторым запросом.
Данных чуть меньше (нет транзитивной проверки типа `P31/P279*`, поэтому тип считается
неподтверждённым, а не ложным), но профиль собирается. Покрыто тестами
`test_details_fall_back_to_action_api` и `test_resolve_survives_sparql_outage`.

**Confidence Score.** Взвешенная сумма улик, каждая видна в карточке фото:

| Улика | Вес | Что значит |
|-------|-----|-----------|
| Геотег | 0.30 | расстояние от геотега фото до координат кампуса (дальше 3 км → отклоняем) |
| Источник | 0.25 | официальный сайт вуза (1.0) > Commons (0.8) > `.edu` (0.75) > неизвестный (0.3) > сток (0.0 → отклоняем) |
| Привязка к вузу | 0.25 | файл лежит в категории Commons самого вуза, а не просто найден рядом |
| Классификатор | 0.20 | уверенность классификатора в категории |
| Вуз в метаданных | 0.15 | название вуза найдено в имени файла, подписи или категориях Commons |
| Повтор в источниках | 0.10 | одно и то же фото пришло из нескольких сборщиков |
| Свежесть | 0.05 | чем старше снимок, тем меньше баллов даёт эта улика |

Если сигнала нет (например, у фото нет геотега), он **не подменяется средним значением** —
веса пере-нормируются по имеющимся уликам, а в `evidence.notes` пишется, чего не хватило.

Пороги: `≥ 0.62` → «Проверено», `0.38…0.62` → «Требует проверки», ниже → «Отклонено».
Отклонённое фото всегда несёт причину: `not_a_photo`, `stock_domain`, `duplicate`,
`too_far`, `no_license`, `too_small`, `junk_class` (логотип, схема, документ, портрет),
`not_this_university` (ничто не связывает фото с этим вузом) и `low_confidence`.

**Классификация по категориям.** Сейчас работает классификатор по метаданным
(`services/classify.py`): он читает имя файла, подпись и категории Commons на ru/en/kk
и по ним определяет категорию — отдельно ловится мусор из ТЗ (логотипы, гербы, карты,
схемы, дипломы, портреты, скриншоты). Метод честно подписан `category_source: "metadata"`,
а его уверенность ограничена сверху 0.85: эвристика не имеет права быть уверенной
на 100%. CLIP zero-shot станет вторым, более сильным сигналом, а этот останется
дешёвым предварительным фильтром.

---

## Запуск

Нужны Python 3.11+ и Node.js 20+. Всё поднимается двумя терминалами.

### 0. Окружение

```bash
git clone <repo> && cd hackaton
cp .env.example .env     # ОБЯЗАТЕЛЬНО впиши свой контакт в CAMPUSLENS_USER_AGENT
```

> Wikimedia требует осмысленный `User-Agent` с контактом. С дефолтным значением
> запросы начнут получать 429.

### 1. Бэкенд

```bash
cd api
python3 -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

Проверка: <http://localhost:8000/api/health> → `{"status":"ok",...}`,
интерактивные доки — <http://localhost:8000/docs>.

### 2. Фронтенд

```bash
cd web
npm install
npm run dev
```

Открыть <http://localhost:3000>. Если бэкенд не на `localhost:8000`, создай `web/.env.local`:

```
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

### 3. Тесты

```bash
cd api
pip install -r requirements-dev.txt
pytest -q          # 60 тестов, сеть не используется
```

```bash
cd web
npm run lint && npm run build
```

---

## Результаты замера точности

Запуск: `cd api && python -m eval.run_eval`

Контрольный набор — 16 размеченных файлов (8 должны попасть в профиль, 8 нет):

| Что меряем | precision | recall | F1 |
|---|---|---|---|
| Решение «показать или отклонить» | 100% | 100% | 100% |
| Категории кампуса (macro-F1) | — | — | 94.7% |

Что это значит: из показанного подходит всё, и ничего подходящего не потеряно.
Пороги зашиты в тест `tests/test_eval.py`, поэтому правка, которая уронит точность,
уронит и сборку.

**Замер сразу окупился.** На первом прогоне precision был 72.7%: три файла,
найденных только геопоиском, проходили в выдачу — жилой дом, безымянный `IMG_0042.jpg`
и, хуже всего, **ворота другого университета**. Последнее — ровно та ошибка, которую
ТЗ называет худшей. Правило ужесточено: файл, который не лежит в категории вуза,
не найден поиском по названию и не упоминает вуз в метаданных, в профиль не попадает
независимо от балла. Исключение — виды города: они и не претендуют на принадлежность
вузу.

Чтобы померить на настоящих файлах:

```bash
cd api
python -m eval.run_eval --template Q1798175 > eval/real.json
# проставить expected_keep и expected_category руками
python -m eval.run_eval --dataset eval/real.json
```

---

## Тесты

```bash
cd api && .venv/bin/python -m pytest -q        # 178 тестов конвейера, без сети (respx)
cd web && npm run e2e                          # 30 браузерных тестов (Playwright)
```

Браузерные тесты поднимают продакшен-сборку на порту 3100 и подменяют все
запросы к API через `page.route()` — бэкенд для них не нужен. Они проверяют
то, чего не видит pytest: что данные доехали до экрана. Один раз это уже
спасло проект — SSE не доходил до интерфейса, потому что `sse-starlette`
разделяет события через CRLF, а парсер ждал `\n\n`.

Отдельный файл `web/e2e/copy.spec.ts` сторожит формулировки: нет пометки
«может быть устаревшим», нет эмодзи, нет сводной оценки безопасности, оценка
по прямой подписана как оценка, цена подана цитатой со ссылкой. Любое из
этих решений легко вернуть случайной правкой, а все они держат главный
принцип проекта.

Если Chromium уже есть в системе (образ Claude Code, часть CI):

```bash
PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium npm run e2e
```

Иначе — один раз `npx playwright install chromium`.

---

## Логотип и заставка

Логотип — `web/public/logo.svg`, вектор: одинаково резкий и в шапке, и во
вкладке браузера, и на иконке приложения. Из него же собраны растровые
копии в `web/public/`: `apple-icon.png` (домашний экран iOS), `icon-512.png`
и `og.png` (превью ссылки в мессенджерах). Пересобрать их можно, отрисовав
`logo.svg` в браузере — исходник один, дубликатов руками не держим.

Заставка (`web/components/Intro.tsx`) показывает `public/intro.mp4` **один
раз на браузер**: флаг в `localStorage`. Пропускается кнопкой, Esc и кликом
мимо; не запускается при `prefers-reduced-motion: reduce`; при ошибке
загрузки закрывается сама, а не висит чёрным экраном. Жюри не должно ждать
ролик, чтобы дойти до поиска.

---

## Тестовый сценарий

0. Войти в аккаунт: сайт закрыт входом. Демо-доступ по умолчанию —
   `jury@locus.kz` / `locus2026demo` (задаётся переменной `CAMPUSLENS_DEMO_ACCOUNT`,
   аккаунт создаётся при старте сервиса). Либо зарегистрироваться на `/login`.
1. Ввести **`КБТУ`** (или нажать готовый пример). С третьей буквы поле показывает
   подсказки из Wikidata — выбор из списка сразу даёт QID и пропускает повторный поиск.
2. Запрос уходит в Wikidata. Аббревиатура разворачивается в варианты (`КБТУ` → `KBTU` →
   `Kazakh-British Technical University` + транслитерация), поиск идёт по `ru`, `en`, `kk`.
3. Если запрос подошёл нескольким записям — появляется список кандидатов с городом,
   координатами и отметкой, есть ли категория Commons. Выбираем нужный.
4. Дальше идёт живая воронка с секундомером:
   `Вуз определён → Commons: категория… N файлов → Commons: геопоиск… M файлов →
   Найдено → Дубли удалены → Отклонено → Проверено → Готово за X.X с`.
5. В галерее — вкладки **Проверено / Требует проверки / Отклонено**, фильтры по категориям.
6. Нажать **«Показать улики»** на любой карточке: видно расстояние геотега до кампуса,
   доверие к домену, привязку к категории вуза, число источников и итоговый балл.
7. Клик по фото открывает большой просмотр: полный разбор Confidence Score,
   все метаданные и кнопка «Открыть источник» на Commons.
8. Переключатель **«Карта»** показывает кампус и точки фото на OpenStreetMap:
   цвет точки — зона достоверности, в попапе миниатюра, расстояние до кампуса
   и ссылка на источник.
9. Вкладка **«Сравнить вузы»** (`/compare`) — два профиля собираются параллельно,
   в таблице видно, у кого больше подтверждённых фото, выше средний Confidence
   и шире охват категорий.
10. Повторный запрос того же вуза отрабатывает заметно быстрее — работает кеш.
    Его статистику видно в `/api/health`.

Проверки «на честность»:

- логотипы `.svg`, слишком мелкие файлы и файлы без лицензии уходят во вкладку
  **«Отклонено»** с причиной;
- фото с геотегом дальше 3 км от кампуса отклоняется как `too_far`;
- один и тот же файл, найденный и по категории, и по геопоиску, схлопывается
  в один — и получает улику «повтор в источниках»;
- если Wikidata недоступна, сервис пишет «источник не ответил», а **не**
  «такого вуза нет»;
- логотип отличается от фотографии не только по расширению: классификатор
  метит его как «мусор» отдельной причиной `junk_class`;
- фото, найденное только геопоиском и никак не связанное с вузом, отклоняется
  с причиной «не тот вуз», а не с общим «низкий балл»;
- пустая категория объясняет, почему она пустая.

Быстрая проверка API без фронта:

```bash
curl "http://localhost:8000/api/resolve?q=КБТУ" | jq
curl -N "http://localhost:8000/api/profile?q=Nazarbayev%20University"   # SSE-поток
curl "http://localhost:8000/api/profile.json?id=Q1798175" | jq '.stats' # тот же конвейер одним ответом
```

---

## Деплой

Пошаговая инструкция со скриншот-путём и разбором типовых ошибок — в [DEPLOY.md](DEPLOY.md).

- **Фронтенд — Vercel.** Импортировать репозиторий, root directory: `web`.
  Переменная окружения: `NEXT_PUBLIC_API_BASE_URL` = адрес развёрнутого API.
- **Бэкенд — Render или Railway.** В корне лежит `render.yaml`, бэкенд собирается
  из `api/Dockerfile` и слушает `$PORT`. Обязательно задать `CAMPUSLENS_USER_AGENT`
  (с контактом) и `CAMPUSLENS_CORS_ORIGINS` (адрес фронта на Vercel), иначе браузер
  заблокирует запросы.

```bash
docker build -t campuslens-api ./api      # локальная проверка образа
docker run -p 8000:8000 --env-file .env campuslens-api
```

---

## API

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/api/health` | health-check |
| `GET` | `/api/resolve?q=&limit=` | кандидаты-вузы из Wikidata, флаг `ambiguous` |
| `GET` | `/api/profile?q=` или `?id=Q…` | **SSE**: поток этапов; `verified` несёт фото (`partial: true`), `done` — профиль с описанием |
| `GET` | `/api/profile.json?q=` или `?id=Q…` | тот же конвейер одним JSON-ответом (для curl и тестов) |
| `GET` | `/api/compare?a=&b=` (или `a_id`/`b_id`) | сравнение двух вузов, оба профиля собираются параллельно |
| `POST` | `/api/subscribe` | заявка «не нашли свой вуз» |
| `POST` | `/api/auth/register` · `/api/auth/login` | регистрация и вход, ответ — токен сессии |
| `GET` | `/api/auth/me` | текущий пользователь по `Authorization: Bearer` |
| `POST` | `/api/uploads` | фото от студента (токен либо `X-Device-Id`) |
| `GET` | `/api/uploads` · `/api/wallet` | свои фото и баланс бонусов |

Параметр `qid` поддерживается как синоним `id` — в ТЗ эндпоинт описан именно так.

> **Сайт закрыт входом.** Поиск, профили и сравнение доступны после регистрации.
> Для жюри задан демо-аккаунт — логин и пароль в разделе «Тестовый сценарий».
> Отключается переменной `CAMPUSLENS_REQUIRE_AUTH=false`.

**Страницы сайта:** `/` — поиск и лендинг, `/u/<QID>` — постоянная ссылка на профиль
(её можно отправить, она пересоберёт профиль сама), `/compare` — сравнение двух вузов,
`/my` — свои фото и бонусы, `/login` — вход и регистрация.

---

## Источники данных

- **[Wikidata](https://www.wikidata.org)** — `wbsearchentities` (поиск по названиям и алиасам
  на ru/en/kk) и SPARQL-эндпоинт `query.wikidata.org`. Берём: QID, название, описание,
  город (P131), страна (P17), координаты (P625), официальный сайт (P856),
  категория Commons (P373), логотип (P154), дата основания (P571).
  Тип сущности проверяется обходом `P31/P279*` до «учебного заведения» — чтобы в выдачу
  не попадали одноимённые компании и станции метро. Данные Wikidata — CC0.
  Если SPARQL недоступен (403 под нагрузкой), те же свойства берутся из Action API
  (`wbgetentities`) — профиль собирается и без него.
- **[OpenStreetMap](https://www.openstreetmap.org)** — Overpass API: объекты в радиусе
  1200 м от координат кампуса (остановки и метро, еда, продукты, аптеки, спорт, парки,
  общежития). Расстояние и время считаются по прямой и так и подписаны — маршрутов мы
  не строим. Пустая группа выводится честно: «в OSM ничего не отмечено», это про полноту
  карты, а не про местность. Данные OSM — ODbL.
- **[Wikimedia Commons](https://commons.wikimedia.org)** — `list=categorymembers`
  (обход категории вуза и подкатегорий), `list=geosearch` (файлы с геотегом в радиусе),
  `prop=imageinfo|coordinates` c `extmetadata` для автора, лицензии, даты и GPS.
  Лицензия каждого файла своя и показана в карточке.

**Чего мы принципиально не показываем.** Сводной оценки безопасности района,
среднего чека в столовой, цены аренды рядом с вузом, наличия розеток в аудиториях,
числа мест в комнате общежития и видео со сторонних площадок. Не потому, что лень:
таких данных в открытых проверяемых источниках нет, а робots.txt и правила VK, TikTok
и YouTube скрейпинг запрещают. Любая из этих цифр была бы выдумкой, поданной как факт, —
ровно то, что ТЗ запрещает в первом же правиле. Там, где вуз публикует цены сам,
мы цитируем его дословно и со ссылкой.

Оба API — публичные, ключи не нужны. Опциональные источники шага 8 (Brave Search / SerpAPI /
LLM) требуют ключей и читаются только из `.env`.

---

## AI и внешние API

- **Wikidata Query Service (SPARQL)** — метаданные вуза.
- **MediaWiki API (Commons)** — поиск и метаданные файлов.
- **rapidfuzz** — нечёткое сопоставление запроса с названиями и алиасами, сравнение
  аббревиатуры с инициалами названия.
- **Классификатор по метаданным** (свой, `services/classify.py`) — категория фото
  по имени файла, подписи и категориям Commons; он же отсекает мусор.
  Это правила, а не модель.
- **imagehash (pHash)** — перцептивная дедупликация по миниатюрам. Классический алгоритм.
- **OpenStreetMap + Leaflet** — карта кампуса с точками фото.
- **CLIP / SigLIP zero-shot** (шаг 5, планируется) — классификация по пикселям
  и более надёжный отсев «мусора».
- **LLM-описание кампуса** (шаг 6) — три провайдера на выбор, определяется по тому,
  какой ключ задан (порядок автовыбора: gemini → groq → anthropic), либо явно через
  `CAMPUSLENS_LLM_PROVIDER`:
  **Gemini** (`GOOGLE_API_KEY`, `CAMPUSLENS_GEMINI_MODEL`, по умолчанию `gemini-3.6-flash`),
  **Groq** (`GROQ_API_KEY`, `CAMPUSLENS_GROQ_MODEL`) или
  **Claude** (`ANTHROPIC_API_KEY`, `CAMPUSLENS_LLM_MODEL`).
  Без ключа описание просто не появляется, профиль собирается как обычно.

  > Источники для описания: данные вуза из Wikidata, подписи к найденным фотографиям
  > и **текст со страниц официального сайта** («О кампусе», «Общежития» и подобных).
  > Обход сайта общий со сборщиком фотографий и закеширован, поэтому второй раз
  > в сеть сервис не ходит.
  >
  > Путь Gemini проверен живым вызовом к API. Путь Groq покрыт тестами на уровне
  > кода, но сетевой вызов к `api.groq.com` из среды разработки был недоступен,
  > поэтому первый запуск с настоящим ключом стоит проверить руками. Если модель
  > выведена из обращения, сервис не падает: ошибка пишется в лог, а профиль
  > собирается без описания. Актуальные модели — console.groq.com/docs/models.

> **Как мы не даём LLM выдумывать.** В модель уходит пронумерованный список фактов,
> собранных нашим же конвейером, и ничего больше — ни общих знаний, ни веб-поиска.
> Ответ приходит структурированным: текст плюс список утверждений с идентификатором
> источника. Каждую ссылку сервис проверяет на своей стороне: утверждение,
> сославшееся на источник, которого мы не давали, отбрасывается и в текст не попадает,
> а в профиль пишется, сколько таких утверждений было. Если подтверждённых утверждений
> не осталось, описание помечается как недостаточное и не показывается.

> **Где в проекте по-прежнему нет ИИ.** Категории фото определяет поиск ключевых слов
> по метаданным, а не нейросеть: CLIP не подключён (причины — в ограничениях).
> Похожесть названий — rapidfuzz, дубли — перцептивный хеш. Это классические алгоритмы.

> **Использование AI-ассистента.** Код этого репозитория написан при активном участии
> AI-ассистента (Claude Code): архитектура, реализация бэкенда и фронтенда, тесты
> и документация создавались в паре с моделью, вся логика проверялась и запускалась
> вручную. Сам сервис при этом не генерирует изображения и не выдумывает данные —
> он только ищет, фильтрует и объясняет то, что нашёл в открытых источниках.

---

## Ограничения и что дальше

| Что | Почему | Как снимается |
|---|---|---|
| Категории определяют правила по метаданным, а не CLIP | веса модели (~350 МБ) и torch (~554 МБ) не помещаются в 512 МБ RAM бесплатного тарифа | инстанс от 2 ГБ либо вынос модели в отдельный сервис |
| Сравнение CLIP-эмбеддингов не реализовано | то же | там же |
| Основной источник — Wikimedia | у вузов без категории Commons и координат данных меньше; частично закрыто поиском по названию и сайтом вуза | поисковый API картинок (шаг 8) |
| Геопоиск захватывает и соседние здания | радиус 1 км вокруг координат | отсев по улике «вуз в метаданных» уже работает, дальше — CLIP |
| Аккаунты, кеш и загруженные фото живут до передеплоя | диск контейнера эфемерный | Postgres и объектное хранилище |
| Отзывы студентов не собираются | у 2GIS и Google Maps нет открытого API, скрейпинг нарушил бы их правила и требование ТЗ про robots.txt | официальный API при наличии ключа |
| Бюджет 25 с жёсткий | требование ТЗ «не позже 30 секунд» | — |
| Сервис не работает офлайн | осознанно: офлайн-заглушки означали бы показ выдуманных данных | — |

Отдельно по замеру точности: цифры выше получены на контрольном наборе, записи
которого написаны вручную и моделируют соглашения Commons. Это не выборка из
настоящего Commons — для замера на живых данных есть режим `--template`.

Wikimedia ограничивает анонимные запросы, поэтому `CAMPUSLENS_USER_AGENT`
с контактом обязателен.

## Структура репозитория

```
.
├── api/                     backend (FastAPI)
│   ├── app/
│   │   ├── config.py        настройки из .env
│   │   ├── models.py        pydantic-схемы
│   │   ├── pipeline.py      конвейер + SSE-этапы
│   │   ├── routers/         /api/resolve, /api/profile
│   │   └── services/        wikidata, commons, textmatch, classify, dedup,
│   │                        evidence, cache, http
│   ├── Dockerfile           образ для Render/Railway
│   └── tests/               pytest + respx (60 тестов, без сети)
├── web/                     frontend (Next.js + Tailwind)
│   ├── app/                 / , /u/[qid] , /compare
│   ├── components/          SearchBar, CandidatePicker, Funnel, ProfileView,
│   │                        PhotoCard, Lightbox, CampusMap, SiteHeader
│   └── lib/                 типы, клиент API (в т.ч. разбор SSE), общий хук
├── render.yaml              деплой бэкенда
├── CLAUDE.md                полное ТЗ кейса
├── .env.example             шаблон окружения (реальный .env в git не попадает)
└── README.md
```

## Секреты

Ключей для базовой работы не требуется. Всё, что появится (Brave Search, SerpAPI,
LLM), читается только из `.env`, который добавлен в `.gitignore`. В репозитории лежит
лишь `.env.example` с пустыми значениями.
